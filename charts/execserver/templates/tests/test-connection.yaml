apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "execserver.fullname" . }}-test-connection"
  labels:
    {{- include "execserver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "execserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
